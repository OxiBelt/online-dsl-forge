# online-dsl-forge-sema

Semantic analysis crate for `online-dsl-forge`.

This crate validates parsed expressions against host schemas and security
profiles, then emits verified programs for bounded runtime evaluation.
